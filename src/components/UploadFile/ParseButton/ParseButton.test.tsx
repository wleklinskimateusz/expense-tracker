import React from "react";
import { screen } from "@testing-library/react";
import { ParseButton } from "./ParseButton";

describe(`<${ParseButton.displayName} />`, () => {
  it("render component", () => {});
});
